# --- DATABASE CONFIGURATION (Prisma & MongoDB) ---
# This is the connection URL for your MongoDB Atlas cluster.
# TEMPLATE: mongodb+srv://<username>:<password>@<cluster_id>.mongodb.net/<database_name>?retryWrites=true&w=majority
# NOTE 1: Your database name is set to 'forms_and_posts_db'.
# NOTE 2: If your password contains special characters (like @ or #), you MUST URL-encode them.
# For example, 'Test@1234' must be entered as 'Test%401234'.
# DATABASE_URL="mongodb+srv://<YOUR_DB_USER>:<YOUR_DB_PASSWORD_ENCODED>@cluster0.aviqifb.mongodb.net/forms_and_posts_db?retryWrites=true&w=majority"

# --- EMAIL CONFIGURATION (Nodemailer) ---
# These are the credentials for your email service provider (e.g., Gmail, SendGrid, Outlook).
# IMPORTANT: If using Gmail or similar, you MUST generate an App Password and use it for SMTP_PASS.
# Regular account passwords will be rejected.
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587" # Use 465 for SSL (secure: true), or 587 for standard TLS (secure: false/default)
# SMTP_USER="your-email-address@example.com"
# SMTP_PASS="your-app-specific-password-secret"

# The email address used to send the notification from (should match SMTP_USER for most services)
# FROM_EMAIL="noreply@yourdomain.com"

# The email address that receives the form submission notification
# TO_EMAIL="your-admin-inbox@example.com"